<h1>Edit Topic:</h1>

<%= render partial: "layouts/errors", locals: {object: @topic} %>

<%= form_for(@topic) do |f|%>

  <%= f.label :group %>
  <%= f.collection_select :group_id, Group.all, :id, :title %>

  <%= f.label :title %>
  <%= f.text_field :title %>


  <%= f.submit %>

<% end %>